---
title: CSD Configuration
sidebar:
  order: 4
  label: CSD Configuration
---

import { Aside, Steps } from "@astrojs/starlight/components";

CSD is already enabled on the demo application. This section shows the configuration screens.

## Navigate to Client-Side Defense

<Steps>
1. Log in to the **F5 Distributed Cloud Console**
2. Select **Client-Side Defense** under **Common Services**
</Steps>

{/* ![CSD Dashboard](/csd/images/csd-dashboard.png) */}

## Domain Configuration

The CSD configuration page lists protected domains. Each domain has a JavaScript injection scope:

| Mode | Description |
| --- | --- |
| **All pages** | CSD script injected on every page |
| **All pages with exceptions** | Exclude specific URL paths |
| **Custom** | Inject only on specified paths |

The demo domain (`botdemo.sales-demo.f5demos.com`) is set to **All pages**.

<Aside type="note">
Do not modify the demo domain configuration. Review the settings in read-only mode.
</Aside>

## HTTP Load Balancer Method

CSD can also be enabled on an HTTP Load Balancer:

<Steps>
1. Navigate to **Multi-Cloud App Connect** &rarr; **HTTP Load Balancers**
2. Edit the load balancer serving the application
3. Under **Common Security Controls**, find **Client-Side Defense**
4. Toggle to **Enabled** and configure injection settings
</Steps>

{/* ![Load Balancer CSD Configuration](/csd/images/csd-lb-config.png) */}

Both the CSD tile and the HTTP LB configuration produce the same result.

## Verify Script Injection

<Steps>
1. Open the demo application in Chrome
2. Press **F12** to open DevTools
3. Go to the **Elements** tab
4. Search for `f5csd` in the HTML
5. Confirm a `<script>` tag referencing the CSD library is present
</Steps>

<Aside type="caution">
If the script is not present, verify the domain is configured and you are accessing the application through the F5 XC load balancer.
</Aside>

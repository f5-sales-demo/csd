---
title: Diagnósticos y verificación
description: >-
  Comprobaciones de estado, pruebas de conectividad y procedimientos de
  verificación para el despliegue de CSD
sidebar:
  order: 8
  label: Diagnósticos y verificación
i18n:
  sourceHash: 754ac3ee6525
  translator: machine
---

import { Aside, Steps } from "@astrojs/starlight/components";

Esta página proporciona una **matriz de verificación UAT por capas** para validar su despliegue de Client-Side Defense de extremo a extremo. Cada caso de prueba sigue la cadena de dependencias de infraestructura — desde la resolución DNS hasta la telemetría de CSD — para que pueda demostrar sistemáticamente que cada componente funciona correctamente.

Estos comandos son el **equivalente por API** del [Recorrido por la consola CSD](/csd/csd-console/) — utilícelos cuando necesite verificar desde la terminal, automatizar el monitoreo o demostrar las capacidades de CSD sin la interfaz de usuario.

## Requisitos previos

Configure sus variables de entorno como se describe en [Automatización API &mdash; Configuración del entorno](/csd/demo/#environment-setup):

```bash
set -a && source .env && set +a
```

Todos los comandos a continuación utilizan el formato de marcador de posición `xTOKENx`. Sustitúyalos con sus variables de entorno (`$F5XC_API_TOKEN`, `$F5XC_NAMESPACE`, etc.) o utilice el formulario interactivo en la parte superior de la página.

## Notas sobre el comportamiento de la API

<Aside type="note">
**PUT y DELETE devuelven `\{\}` vacío** con HTTP 200 — esta es la respuesta de éxito esperada, no un error. Solo **POST** devuelve el objeto creado. Los **endpoints de listado** (p. ej., `/healthchecks`) y los **endpoints GET individuales** (p. ej., `/healthchecks/\{name\}`) devuelven estructuras de respuesta diferentes — consulte [Referencia de API &mdash; Convenciones de API](/csd/api-reference/#api-conventions) para conocer las rutas `jq` correctas para cada uno.
</Aside>

## Utilidades de rango temporal

Muchos endpoints de CSD requieren marcas de tiempo epoch (segundos desde la época Unix). Estos comandos de una línea calculan los tiempos de inicio/fin para rangos comunes.

**Multiplataforma** (macOS + Linux):

```bash
# Current time as epoch seconds
NOW=$(date +%s)

# 1 hour ago
START_1H=$(( NOW - 3600 ))

# 24 hours ago
START_24H=$(( NOW - 86400 ))

# 7 days ago
START_7D=$(( NOW - 604800 ))

# 30 days ago
START_30D=$(( NOW - 2592000 ))
```

| Preconfiguración | Segundos | Expresión shell |
| --- | --- | --- |
| 1 hora | 3.600 | `$(( $(date +%s) - 3600 ))` |
| 24 horas | 86.400 | `$(( $(date +%s) - 86400 ))` |
| 7 días | 604.800 | `$(( $(date +%s) - 604800 ))` |
| 30 días | 2.592.000 | `$(( $(date +%s) - 2592000 ))` |

<Aside type="note">
Los endpoints de CSD (`/api/shape/csd/`) utilizan **segundos epoch** (p. ej., `1710000000`). Los endpoints de logs de acceso (`/api/data/`) utilizan marcas de tiempo **ISO 8601** (p. ej., `2024-03-10T00:00:00Z`). Los comandos en cada sección usan el formato correcto para ese endpoint.
</Aside>

## Formato de los casos de prueba

Cada prueba a continuación sigue esta estructura:

| Campo | Descripción |
| --- | --- |
| **ID de prueba** | Número de capa + ID secuencial (p. ej., DNS-1, TLS-2) |
| **Qué demuestra** | El hecho específico de infraestructura que se verifica |
| **Comando** | Comando `curl` o `dig` listo para ejecutar |
| **APROBADO / FALLIDO** | Salida esperada para estado saludable vs. no saludable |
| **Corrección** | Enlace a la sección de configuración o solución de problemas correspondiente |

---

## Capa 1: Resolución DNS

El DNS es la base — si el dominio no resuelve a la VIP del balanceador de carga, nada más funciona.

### DNS-1: Resolución de registro A

**Qué demuestra:** El dominio resuelve a la dirección IP VIP del balanceador de carga.

```bash
dig +short xF5XC_DOMAINNAMEx A
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; IP VIP devuelta (p. ej., `72.19.3.185`) | El dominio resuelve a la IP virtual del LB |
| **FALLIDO** &mdash; respuesta vacía | El registro A de DNS no está configurado |

**Corrección:** [Automatización API &mdash; Paso 4: Configurar DNS](/csd/demo/phase-1-build/#step-4-configure-dns)

<Aside type="tip">
**Caché DNS negativa:** Si el dominio previamente devolvió `NXDOMAIN`, los resolvers recursivos pueden almacenar ese resultado en caché. Verifique contra el servidor de nombres autoritativo para descartar caché obsoleta: `dig +short xF5XC_DOMAINNAMEx A @ns1.f5clouddns.com` o use un resolver público como `@8.8.8.8`.
</Aside>

### DNS-2: Registro de desafío ACME

**Qué demuestra:** El registro de desafío ACME existe para el aprovisionamiento automático de certificados TLS.

```bash
dig +short _acme-challenge.xF5XC_DOMAINNAMEx CNAME
dig +short _acme-challenge.xF5XC_DOMAINNAMEx TXT
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; CNAME hacia `*.autocerts.ves.volterra.io.` (DNS externo) | El CNAME del desafío ACME está configurado |
| **APROBADO** &mdash; Registro TXT con valor del dominio (DNS gestionado por F5 Distributed Cloud (F5 XC)) | Desafío ACME gestionado por la plataforma mediante registro TXT |
| **FALLIDO** &mdash; ambos vacíos | Registro ACME no configurado; el certificado permanecerá en `PreDomainChallengePending` |

<Aside type="note">
Cuando se utiliza DNS gestionado por F5 XC con `allow_http_lb_managed_records` habilitado, la plataforma crea automáticamente un registro **TXT** (no un CNAME) en el grupo de conjuntos de registros RR `x-ves-io-managed`. Esta validación ACME basada en TXT se gestiona internamente. Cuando se utiliza DNS externo, debe crear manualmente un CNAME hacia `*.autocerts.ves.volterra.io`.
</Aside>

**Corrección:** Para DNS externo, cree el CNAME: `_acme-challenge.xF5XC_DOMAINNAMEx` &rarr; `*.autocerts.ves.volterra.io`. Para DNS gestionado por F5 XC, habilite `allow_http_lb_managed_records` en la zona DNS &mdash; consulte [DNS-4](#dns-4-registros-gestionados-de-f5-xc-habilitados). Consulte [Automatización API &mdash; Paso 4](/csd/demo/phase-1-build/#step-4-configure-dns).

### DNS-3: Autoridad del servidor de nombres

**Qué demuestra:** Si F5 XC es el proveedor DNS autoritativo para el dominio raíz.

```bash
dig +short NS xF5XC_ROOT_DOMAINx
```

| Resultado | Significado |
| --- | --- |
| Incluye `ns1.f5clouddns.com` y `ns2.f5clouddns.com` | DNS gestionado por F5 XC &mdash; los registros pueden crearse automáticamente |
| Otros servidores de nombres | DNS externo &mdash; los registros deben crearse manualmente |

**Corrección:** [Automatización API &mdash; Detectar autoridad DNS](/csd/demo/phase-1-build/#detect-dns-authority)

### DNS-4: Registros gestionados de F5 XC habilitados

**Qué demuestra:** La zona DNS de F5 XC permite la creación automática de registros para balanceadores de carga (solo DNS gestionado por F5 XC).

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/dns/namespaces/system/dns_zones/xF5XC_ROOT_DOMAINx" \
  | jq '.spec.primary.allow_http_lb_managed_records'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `true` | La plataforma creará automáticamente registros A y ACME para los LBs |
| **FALLIDO** &mdash; `false` o `null` | Registros gestionados deshabilitados; habilítelos mediante actualización de la zona |

<Aside type="caution">
Un resultado `false` es una configuración errónea común cuando se utiliza DNS gestionado por F5 XC. Sin los registros gestionados habilitados, la plataforma no puede crear automáticamente el registro A (apuntando a la VIP) ni el CNAME del desafío ACME (requerido para el aprovisionamiento de certificados TLS). Esto causa que el balanceador de carga permanezca atascado en `VIRTUAL_HOST_PENDING_A_RECORD` y el certificado se quede en `PreDomainChallengePending`. Habilite los registros gestionados en la zona DNS o cree los registros manualmente.
</Aside>

**Corrección:** [Automatización API &mdash; Opción A: DNS gestionado por F5 XC](/csd/demo/phase-1-build/#option-a-f5-xc-managed-dns)

---

## Capa 2: Certificado TLS

Después de que el DNS resuelve, el balanceador de carga debe tener un certificado TLS válido.

### TLS-1: Estado del certificado

<Aside type="note">
**Esta prueba aplica únicamente al LB HTTPS (`${F5XC_LB_NAME}-https`).** El LB HTTP no tiene certificado. En entornos de demostración, TLS-1 es informativo — si muestra `AutoCertDomainRateLimited`, la demostración procede normalmente utilizando URLs `http://` a través del LB HTTP.
</Aside>

**Qué demuestra:** El certificado TLS automático ha sido emitido en el LB HTTPS.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-https" \
  | jq '.spec.cert_state'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `"CertificateValid"` o `"AutoCertRenewing"` | El certificado es válido y está activo |
| **ADVERTENCIA** &mdash; `"DomainChallengePending"` o `"DomainChallengeStarted"` | Desafío ACME en progreso &mdash; espere 5&ndash;10 minutos |
| **INFO** &mdash; `"AutoCertDomainRateLimited"` | Límite de velocidad de Let's Encrypt alcanzado &mdash; esperado en entornos de demostración, no afecta al LB HTTP |
| **FALLIDO** &mdash; `"PreDomainChallengePending"` | Registro de desafío ACME faltante &mdash; consulte [DNS-2](#dns-2-registro-de-desafío-acme) |

**Corrección:** Asegúrese de que el registro de desafío ACME esté configurado (CNAME para DNS externo, o habilite registros gestionados para DNS F5 XC). El aprovisionamiento del certificado toma **5&ndash;10 minutos** después de configurar el registro. Si permanece atascado más de 15 minutos, consulte [Certificado atascado &mdash; Recreación limpia](/csd/demo/#certificate-stuck--clean-recreation-optional). Si está limitado por velocidad, el LB HTTP no se ve afectado.

### TLS-2: Detalles del certificado

**Qué demuestra:** El certificado cubre el dominio esperado y muestra información de expiración. Solo aplica al LB HTTPS.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-https" \
  | jq '{
    cert_state: .spec.cert_state,
    auto_cert_info: .spec.auto_cert_info
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `auto_cert_info` contiene `dns_records` y metadatos del certificado | Detalles de aprovisionamiento del certificado disponibles |
| **FALLIDO** &mdash; `auto_cert_info` es `null` o está vacío | Certificado aún no aprovisionado |

### TLS-3: Handshake TLS en vivo

**Qué demuestra:** El certificado TLS es válido y el handshake se completa desde el lado del cliente. Solo aplica cuando el LB HTTPS tiene un certificado válido.

<Aside type="note">
En entornos de demostración, use `curl http://xF5XC_DOMAINNAMEx/` (HTTP, no HTTPS) como la ruta de verificación principal. La prueba de handshake TLS es opcional — solo funciona cuando el certificado del LB HTTPS está en `CertificateValid`.
</Aside>

```bash
curl -sv "https://xF5XC_DOMAINNAMEx/" 2>&1 \
  | grep -E 'SSL connection|subject:|expire date:|issuer:'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; muestra `SSL connection using TLSv1.3`, sujeto y expiración válidos | TLS de extremo a extremo está funcionando |
| **FALLIDO** &mdash; conexión rechazada o error de certificado | Verifique la resolución DNS y el estado del certificado; si está limitado por velocidad, use HTTP en su lugar |

### TLS-4: Destino del registro ACME desde el LB

**Qué demuestra:** El balanceador de carga HTTPS reporta el destino ACME correcto para la validación del certificado.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-https" \
  | jq '{
    vip_ip: .spec.dns_info[0].ip_address,
    acme_target: .spec.auto_cert_info.dns_records
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `vip_ip` y `acme_target` poblados | Los registros DNS pueden verificarse contra estos valores |
| **FALLIDO** &mdash; valores `null` | El LB podría no tener configurado `https_auto_cert` |

---

## Capa 3: Balanceador de carga HTTP

El balanceador de carga debe estar en estado listo y correctamente configurado.

### LB-1: Estado operacional

**Qué demuestra:** El balanceador de carga HTTP (principal) está completamente operativo y aceptando tráfico.

<Aside type="note">
**El LB HTTP es el objetivo de verificación principal.** En entornos de demostración, verifique primero el LB HTTP (`${F5XC_LB_NAME}-http`) — alcanza `VIRTUAL_HOST_READY` tan pronto como el DNS resuelve, sin dependencia de certificado. Use `curl http://xF5XC_DOMAINNAMEx/` para la verificación principal basada en HTTP.
</Aside>

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '.spec.state'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `"VIRTUAL_HOST_READY"` | El LB está operativo |
| **FALLIDO** &mdash; `"VIRTUAL_HOST_PENDING_A_RECORD"` | Registro A de DNS no configurado &mdash; consulte [DNS-1](#dns-1-resolución-de-registro-a) |

**Corrección:** [LB atascado en VIRTUAL_HOST_PENDING_A_RECORD](/csd/demo/#lb-stuck-in-virtual_host_pending_a_record)

### LB-2: Configuración de dominio

**Qué demuestra:** El balanceador de carga HTTP está configurado para el/los dominio(s) correcto(s).

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '.spec.domains'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; el array contiene su FQDN (p. ej., `["app.example.com"]`) | El LB está configurado para el dominio esperado |
| **FALLIDO** &mdash; dominio faltante o incorrecto | Actualice la especificación del LB con el dominio correcto |

### LB-3: CSD habilitado en el LB

**Qué demuestra:** Client-Side Defense está habilitado en el balanceador de carga con la política de inyección JS correcta.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '{
    csd_enabled: (if .spec.client_side_defense then true else false end),
    js_policy: .spec.client_side_defense.policy
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `csd_enabled: true` con `js_insert_all_pages` en la política | CSD activo con inyección JS en todas las páginas |
| **FALLIDO** &mdash; `csd_enabled: false` | CSD no configurado en el LB |

**Corrección:** [Referencia de API &mdash; Habilitar CSD en un balanceador de carga](/csd/api-reference/#enable-csd-on-a-load-balancer)

### LB-4: Pool de rutas predeterminado

**Qué demuestra:** El balanceador de carga hace referencia al pool de origen correcto con el peso y prioridad esperados.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '.spec.default_route_pools[] | {
    pool: .pool.name,
    namespace: .pool.namespace,
    weight: .weight,
    priority: .priority
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; muestra el nombre de su pool de origen con peso/prioridad | El LB está enrutando al backend correcto |
| **FALLIDO** &mdash; vacío o pool incorrecto | Actualice la configuración de `default_route_pools` del LB |

### LB-5: Estado de despliegue (condiciones por sitio)

**Qué demuestra:** El balanceador de carga está desplegado y reporta condiciones saludables en todos los sitios.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '{
    state: .spec.state,
    dns_info: .spec.dns_info,
    host_name: .spec.host_name
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `state` es `VIRTUAL_HOST_READY`, `dns_info` poblado | LB completamente desplegado con VIP asignada |
| **FALLIDO** &mdash; estado pendiente o `dns_info` vacío | Problema de DNS o certificado bloqueando el despliegue |

<Aside type="tip">
Cuando el LB no está en estado `VIRTUAL_HOST_READY`, verifique `.status[].virtual_host_status.error_description` para los mismos detalles de error que se muestran en la interfaz de la consola F5 XC:

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '[.status[]? | select(.virtual_host_status != null) | .virtual_host_status | {state, error_description, suggested_action}]'
```
</Aside>

### LB-6: Resumen completo del LB

**Qué demuestra:** Instantánea completa de ambas configuraciones de LB para depuración.

**LB HTTP (principal):**

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '{
    name: .metadata.name,
    state: .spec.state,
    domains: .spec.domains,
    csd_enabled: (if .spec.client_side_defense then true else false end),
    route_pools: [.spec.default_route_pools[] | .pool.name],
    advertise: (if .spec.advertise_on_public_default_vip then "public_default_vip" else "custom" end)
  }'
```

**LB HTTPS (secundario — incluye estado del certificado):**

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-https" \
  | jq '{
    name: .metadata.name,
    state: .spec.state,
    cert_state: .spec.cert_state,
    domains: .spec.domains,
    csd_enabled: (if .spec.client_side_defense then true else false end),
    route_pools: [.spec.default_route_pools[] | .pool.name]
  }'
```

---

## Capa 4: Pool de origen

El pool de origen define los servidores backend a los que el balanceador de carga enruta el tráfico.

### OP-1: Configuración del pool de origen

**Qué demuestra:** El pool de origen existe con el servidor backend, puerto y algoritmo de balanceo correctos.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/origin_pools/xF5XC_ORIGIN_POOLx" \
  | jq '{
    name: .metadata.name,
    origin_servers: [.spec.origin_servers[] | {
      ip: .public_ip.ip,
      labels: .labels
    }],
    port: .spec.port,
    lb_algorithm: .spec.loadbalancer_algorithm,
    endpoint_selection: .spec.endpoint_selection
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; muestra IP, puerto y algoritmo correctos | Pool de origen configurado correctamente |
| **FALLIDO** &mdash; `404` o valores incorrectos | Pool de origen faltante o mal configurado |

**Corrección:** [Automatización API &mdash; Paso 2: Crear pool de origen](/csd/demo/phase-1-build/#step-2-create-origin-pool)

### OP-2: TLS hacia el origen

**Qué demuestra:** Si TLS está configurado para la conexión entre el LB y el servidor de origen.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/origin_pools/xF5XC_ORIGIN_POOLx" \
  | jq '{
    tls_config: (if .spec.no_tls then "no_tls (plaintext)" elif .spec.use_tls then "use_tls (encrypted)" else "unknown" end)
  }'
```

| Resultado | Significado |
| --- | --- |
| `no_tls (plaintext)` | El LB se conecta al origen por HTTP (esperado para la demo de Juice Shop) |
| `use_tls (encrypted)` | El LB se conecta al origen por HTTPS |

### OP-3: Asociación de healthcheck

**Qué demuestra:** El pool de origen tiene el healthcheck correcto vinculado (si aplica).

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/origin_pools/xF5XC_ORIGIN_POOLx" \
  | jq '.spec.healthcheck'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; array con referencia al healthcheck (nombre, namespace, tipo) | El healthcheck está vinculado |
| **OK** &mdash; array vacío `[]` | Sin healthcheck (aceptable &mdash; CSD no requiere uno) |
| **FALLIDO** &mdash; se esperaba un healthcheck pero el array está vacío | El healthcheck no se encontró al momento de la creación &mdash; consulte [Healthcheck no vinculado](/csd/demo/#healthcheck-not-linked-to-origin-pool) |

### OP-4: Conectividad del origen

**Qué demuestra:** El servidor de origen es accesible desde el lado del cliente (no prueba la ruta LB-a-origen).

```bash
curl -s -o /dev/null -w '%{http_code}' \
  "http://xF5XC_ORIGIN_IPx:xF5XC_ORIGIN_PORTx/"
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `200` (u otro código HTTP válido) | El servidor de origen está respondiendo |
| **FALLIDO** &mdash; `000` o conexión rechazada | Servidor de origen inaccesible desde esta red |

<Aside type="note">
Esto prueba la conectividad directa cliente-a-origen. La ruta LB-a-origen puede usar una ruta de red diferente. Un fallo aquí no necesariamente significa que el LB no pueda alcanzar el origen.
</Aside>

---

## Capa 5: Comprobación de estado

Las comprobaciones de estado monitorean la disponibilidad del backend. Son opcionales para CSD pero útiles para despliegues en producción.

### HC-1: Configuración del healthcheck

**Qué demuestra:** El healthcheck existe con el tipo, ruta, temporización y umbrales correctos.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/healthchecks/xF5XC_HC_NAMEx" \
  | jq '{
    name: .metadata.name,
    type: (if .spec.http_health_check then "HTTP" elif .spec.tcp_health_check then "TCP" else "unknown" end),
    path: .spec.http_health_check.path,
    expected_status: .spec.http_health_check.expected_status_codes,
    timeout: .spec.timeout,
    interval: .spec.interval,
    unhealthy_threshold: .spec.unhealthy_threshold,
    healthy_threshold: .spec.healthy_threshold
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; muestra tipo HTTP con ruta `/` y esperado `200` | Healthcheck configurado correctamente |
| **FALLIDO** &mdash; respuesta `404` | El healthcheck no existe (puede haberse omitido &mdash; consulte [Fase 1 Paso 1](/csd/demo/phase-1-build/#step-1-create-healthcheck-optional)) |

### HC-2: Listar todos los healthchecks

**Qué demuestra:** Enumera todos los healthchecks en el namespace para verificar nombres y cantidad.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/healthchecks" \
  | jq -r '
    ["NAME", "NAMESPACE", "DESCRIPTION"],
    (.items[] | [
      .name,
      .namespace,
      (.description | if length == 0 then "—" else . end)
    ])
    | @tsv' | column -t
```

<Aside type="note">
El endpoint de listado devuelve solo datos resumidos (nombre, namespace, descripción). Para ver los detalles completos de configuración (tipo, ruta, intervalo, timeout), consulte un healthcheck individual usando [HC-1](#hc-1-configuración-del-healthcheck).
</Aside>

---

## Capa 6: Configuración CSD

CSD debe estar habilitado a nivel de tenant y tener configurada la etiqueta de inyección JavaScript.

### CSD-1: Estado CSD del tenant

**Qué demuestra:** CSD está configurado y habilitado para el tenant.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/status" \
  | jq '{isConfigured, isEnabled}'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; ambos `true` | CSD está activo para este tenant |
| **FALLIDO** &mdash; `isConfigured: false` | CSD no habilitado a nivel de tenant &mdash; contacte al administrador de F5 XC |
| **FALLIDO** &mdash; `isEnabled: false` | CSD configurado pero no activo |

### CSD-2: Etiqueta de inyección JS

**Qué demuestra:** La etiqueta de inyección JavaScript de CSD está generada y lista para inyección.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/js_configuration" \
  | jq '{has_script_tag: (.scriptTag | length > 0)}'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `has_script_tag: true` | La etiqueta de inyección JS está configurada |
| **FALLIDO** &mdash; `has_script_tag: false` | Sin etiqueta de script &mdash; verifique que CSD esté habilitado y que haya un dominio protegido registrado |

### CSD-3: Contenido de la etiqueta JS

**Qué demuestra:** Muestra la etiqueta de script completa para verificación o inyección manual.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/js_configuration" \
  | jq '.scriptTag'
```

### CSD-4: Registro de dominio protegido

**Qué demuestra:** El dominio raíz está registrado como dominio protegido de CSD en el tenant.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/protected_domains" \
  | jq '.items[] | {name, namespace, description}'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; muestra un elemento con un `name` no vacío | El dominio está protegido |
| **FALLIDO** &mdash; los elementos tienen campos `name` y `namespace` vacíos | No hay dominios protegidos registrados &mdash; consulte [Fase 1 Paso 6](/csd/demo/phase-1-build/#step-6-register-protected-domain) |

<Aside type="note">
El endpoint de listado de dominios protegidos devuelve datos resumidos. Los elementos con todos los campos vacíos indican que no hay dominios protegidos configurados. La etiqueta JavaScript de CSD ([CSD-2](#csd-2-etiqueta-de-inyección-js)) es un indicador más fiable de que la protección del dominio está activa.
</Aside>

### CSD-5: Verificación de inyección JS en vivo

**Qué demuestra:** El JavaScript de CSD está siendo inyectado efectivamente en las respuestas de página servidas por el balanceador de carga.

```bash
curl -s "http://xF5XC_DOMAINNAMEx/" \
  | grep -oE '(zeronaught|shape)\.com[^"]*' | head -1
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; devuelve un fragmento de URL de `zeronaught.com` o `shape.com` (p. ej., `zeronaught.com/__imp_apg__/js/...`) | El JavaScript de CSD está siendo inyectado en las páginas |
| **FALLIDO** &mdash; salida vacía | JS no inyectado &mdash; verifique [LB-3](#lb-3-csd-habilitado-en-el-lb) y [CSD-1](#csd-1-estado-csd-del-tenant) |

---

## Capa 7: Verificación de tráfico

Verifique que el tráfico en vivo está llegando al balanceador de carga y se está procesando correctamente.

### TV-1: Recuento de solicitudes (últimas 24 horas)

**Qué demuestra:** El tráfico está llegando al balanceador de carga. Cero resultados significa que no llega tráfico.

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{
    "start_time": "'"$(date -u -d '24 hours ago' +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -v-24H +%Y-%m-%dT%H:%M:%SZ)"'",
    "end_time": "'"$(date -u +%Y-%m-%dT%H:%M:%SZ)"'"
  }' \
  "xF5XC_API_URLx/api/data/namespaces/xF5XC_NAMESPACEx/access_logs/aggregation" \
  | jq '{total_requests: .total_hits}'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `total_requests` es una cadena distinta de cero (p. ej., `"380"`) | El tráfico está fluyendo a través del LB |
| **FALLIDO** &mdash; `"0"` o sin datos | No llega tráfico al LB &mdash; verifique [DNS-1](#dns-1-resolución-de-registro-a) y [LB-1](#lb-1-estado-operacional) |

<Aside type="note">
El valor de `total_hits` se devuelve como una **cadena** (p. ej., `"380"`), no como un entero. El namespace se infiere de la ruta de la URL &mdash; no lo incluya en el cuerpo de la solicitud.
</Aside>

### TV-2: Distribución de códigos de estado

**Qué demuestra:** El desglose de códigos de estado de respuesta revela patrones de errores.

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{
    "start_time": "'"$(date -u -d '24 hours ago' +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -v-24H +%Y-%m-%dT%H:%M:%SZ)"'",
    "end_time": "'"$(date -u +%Y-%m-%dT%H:%M:%SZ)"'",
    "sort": "DESCENDING",
    "limit": 100
  }' \
  "xF5XC_API_URLx/api/data/namespaces/xF5XC_NAMESPACEx/access_logs" \
  | jq -r '
    [.logs[] | fromjson | .rsp_code_class]
    | group_by(.) | map({class: .[0], count: length})
    | sort_by(-.count)
    | ["STATUS_CLASS", "COUNT"], (.[] | [.class, .count])
    | @tsv' | column -t
```

<Aside type="note">
El endpoint de agregación no soporta agrupación por `terms` &mdash; `aggs` siempre devuelve vacío. Este comando obtiene las 100 entradas de log más recientes y calcula la distribución del lado del cliente. Cada entrada de log es una **cadena JSON** que debe ser procesada con `fromjson`.
</Aside>

Salida esperada para un sitio saludable:

```
STATUS_CLASS                  COUNT
2xx                           82
downstream_remote_disconnect  18
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; mayoría `2xx` | El sitio está sirviendo respuestas exitosas |
| **ADVERTENCIA** &mdash; alto número de `4xx` | Errores del cliente (URLs incorrectas, recursos faltantes) |
| **FALLIDO** &mdash; alto número de `5xx` | Errores del servidor &mdash; verifique la salud del servidor de origen |

La clase `downstream_remote_disconnect` indica que el cliente cerró la conexión antes de que la respuesta se completara (común para solicitudes de long-polling o actualización de WebSocket).

### TV-3: Muestras de solicitudes recientes

**Qué demuestra:** Las solicitudes individuales se están registrando con los campos correctos.

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{
    "start_time": "'"$(date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -v-1H +%Y-%m-%dT%H:%M:%SZ)"'",
    "end_time": "'"$(date -u +%Y-%m-%dT%H:%M:%SZ)"'",
    "sort": "DESCENDING",
    "limit": 10
  }' \
  "xF5XC_API_URLx/api/data/namespaces/xF5XC_NAMESPACEx/access_logs" \
  | jq -r '
    ["TIMESTAMP", "METHOD", "PATH", "STATUS", "SRC_IP"],
    (.logs[] | fromjson | [.["@timestamp"], .method, .req_path, .rsp_code, .src_ip])
    | @tsv' | column -t
```

<Aside type="caution">
Cada entrada en el array `.logs[]` es una **cadena JSON**, no un objeto procesado. Debe pasar por `fromjson` en jq antes de acceder a los campos (p. ej., `.logs[] | fromjson | .method`).
</Aside>

### TV-4: Inyección JS de CSD en logs de acceso

**Qué demuestra:** Los logs de acceso confirman que el JavaScript de CSD está siendo inyectado en las respuestas.

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{
    "start_time": "'"$(date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -v-1H +%Y-%m-%dT%H:%M:%SZ)"'",
    "end_time": "'"$(date -u +%Y-%m-%dT%H:%M:%SZ)"'",
    "sort": "DESCENDING",
    "limit": 20
  }' \
  "xF5XC_API_URLx/api/data/namespaces/xF5XC_NAMESPACEx/access_logs" \
  | jq '[.logs[] | fromjson | select(.csd_js_injection == "true")] | length as $injected |
    {injected_count: $injected, total_sampled: 20}'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `injected_count` > 0 | El JS de CSD está siendo inyectado en las respuestas de página |
| **FALLIDO** &mdash; `injected_count: 0` | JS no inyectado &mdash; verifique [CSD-1](#csd-1-estado-csd-del-tenant) y [LB-3](#lb-3-csd-habilitado-en-el-lb) |

<Aside type="note">
El campo `csd_js_injection` solo está presente en las entradas de log donde ocurrió la inyección. En pruebas, el 40% de las entradas de log muestreadas tenían `csd_js_injection: "true"` (solicitudes de páginas HTML), mientras que el 60% no tenían el campo (activos estáticos como archivos `.ico`, `.js`, `.css` y conexiones WebSocket). Las solicitudes de páginas HTML deberían mostrar inyección; las llamadas API y solicitudes de activos estáticos típicamente no lo harán.
</Aside>

### TV-5: Prueba de conectividad de extremo a extremo

**Qué demuestra:** Una solicitud completa fluye desde el cliente a través de DNS, LB y origen y devuelve una respuesta válida.

```bash
curl -sv "http://xF5XC_DOMAINNAMEx/" 2>&1 \
  | grep -E 'Connected to|< HTTP|< content-type'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; muestra conexión, HTTP 200 y content-type | Todo el stack está operativo |
| **FALLIDO** &mdash; conexión rechazada o error DNS | Comience la depuración en [Capa 1: DNS](#capa-1-resolución-dns) |

<Aside type="tip">
Para pruebas HTTPS de extremo a extremo (solo cuando el certificado del LB HTTPS es válido), use `curl -sv "https://xF5XC_DOMAINNAMEx/"` y verifique que aparezca `SSL connection` en la salida.
</Aside>

---

## Capa 8: Telemetría CSD y política de dominios

Estos comandos consultan los mismos datos que se muestran en el panel de la [Consola CSD](/csd/csd-console/), la lista de scripts, los campos de formulario y las vistas de red.

### TEL-1: Inventario de scripts

**Qué demuestra:** CSD está detectando y catalogando scripts que se ejecutan en el dominio protegido.

```bash
NOW=$(date +%s)
START=$(( NOW - 604800 ))

curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d "{
    \"startTime\": \"${START}\",
    \"endTime\": \"${NOW}\"
  }" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts" \
  | jq -r '
    ["SCRIPT", "RISK", "STATUS", "FIELDS", "USERS"],
    (.scripts[] | [
      (.script_name | if length > 50 then .[:47] + "..." else . end),
      .risk_level,
      .status,
      (.form_fields_read // 0),
      (.affected_users_count // 0)
    ])
    | @tsv' | column -t
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; scripts listados con niveles de riesgo | CSD está monitoreando activamente los scripts |
| **FALLIDO** &mdash; array vacío | Consulte [Solución de problemas: Array de scripts vacío](#array-de-scripts-vacío) |

<Aside type="tip">
Los nombres de scripts son URLs completas y pueden ser muy largos. El comando anterior trunca los nombres a 50 caracteres para legibilidad en la terminal. Elimine el filtro de truncamiento para ver las URLs completas.
</Aside>

### TEL-2: Dominios detectados

**Qué demuestra:** CSD ha detectado dominios de origen de scripts y los ha clasificado por estado.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/detected_domains" \
  | jq '{
    summary: {
      action_needed: .domain_summary.actionNeededCount.count,
      mitigated: .domain_summary.mitigatedDomains.count,
      allowed: .domain_summary.allowedDomains.count,
      total: .domain_summary.totalDomains.count
    },
    domains: [.domains_list[] | {
      domain: .domain,
      category: .category,
      status: .status,
      first_seen: (.firstSeenDate | tonumber | todate),
      latest_seen: (.latestSeenDate | tonumber | todate)
    }]
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `total` > 0 con dominios listados | CSD está rastreando dominios de scripts |
| **ADVERTENCIA** &mdash; `action_needed` > 0 | Algunos dominios necesitan revisión |
| **FALLIDO** &mdash; respuesta vacía | Sin datos de telemetría &mdash; verifique [CSD-5](#csd-5-verificación-de-inyección-js-en-vivo) |

### TEL-3: Estado de la política de dominios

**Qué demuestra:** Muestra la distribución de dominios permitidos vs. mitigados y si la política es consistente.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/detected_domains" \
  | jq '{
    action_needed: .domain_summary.actionNeededCount.count,
    mitigated: .domain_summary.mitigatedDomains.count,
    allowed: .domain_summary.allowedDomains.count,
    total: .domain_summary.totalDomains.count,
    domains_needing_action: [.domains_list[] | select(.status == "AN") | .domain]
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `action_needed: 0` | Todos los dominios han sido revisados y clasificados |
| **ADVERTENCIA** &mdash; `action_needed` > 0 | Los dominios listados en `domains_needing_action` requieren revisión |

### TEL-4: Inventario de campos de formulario

**Qué demuestra:** CSD ha detectado campos de formulario que los scripts están leyendo, con clasificación de sensibilidad.

```bash
NOW=$(date +%s)
START=$(( NOW - 604800 ))

curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/formFields?startTime=${START}&endTime=${NOW}" \
  | jq -r '
    ["FIELD", "SENSITIVITY", "SCRIPTS"],
    (.form_fields[] | [
      .name,
      .analysis,
      (.scripts_count // 0)
    ])
    | @tsv' | column -t
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; campos de formulario listados con sensibilidad | CSD está rastreando el acceso a campos de formulario |
| **INFO** &mdash; array vacío | No se detectaron campos de formulario (esperado si no hay formularios en el sitio) |

### TEL-5: Análisis detallado de script

**Qué demuestra:** Información detallada sobre un script específico incluyendo riesgo, comportamientos e interacciones de red.

Primero, obtenga un ID de script de [TEL-1](#tel-1-inventario-de-scripts), luego consulte sus detalles:

```bash
SCRIPT_ID="your-script-id"

# Overview (risk level, type, source domain)
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts/${SCRIPT_ID}/dashboard" \
  | jq '{script_name, risk_level, type, source_domain, status}'

# Behaviors over time
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts/${SCRIPT_ID}/behaviors" \
  | jq '.behaviors'

# Network interactions (domains the script communicates with)
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts/${SCRIPT_ID}/networkInteractions" \
  | jq '.network_interactions'
```

### TEL-6: Usuarios afectados

**Qué demuestra:** Lista los usuarios impactados por un script específico, mostrando el alcance de la exposición.

```bash
SCRIPT_ID="your-script-id"
NOW=$(date +%s)
START=$(( NOW - 604800 ))

curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d "{
    \"startTime\": \"${START}\",
    \"endTime\": \"${NOW}\"
  }" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts/${SCRIPT_ID}/affectedUsers" \
  | jq -r '
    ["IP_ADDRESS", "DEVICE_ID", "GEO", "CHANNEL", "USER_AGENT"],
    (.affected_users[] | [
      .ip_address,
      (.device_id | if length > 12 then .[:12] + "..." else . end),
      .geolocation,
      .channel,
      (.user_agent | if length > 30 then .[:27] + "..." else . end)
    ])
    | @tsv' | column -t
```

### TEL-7: Recuento de scripts por nivel de riesgo

**Qué demuestra:** Distribución agregada de riesgo en todos los scripts detectados.

```bash
NOW=$(date +%s)
START=$(( NOW - 604800 ))

curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d "{
    \"startTime\": \"${START}\",
    \"endTime\": \"${NOW}\"
  }" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts" \
  | jq '[.scripts[] | .risk_level] | group_by(.) | map({risk_level: .[0], count: length}) | sort_by(-.count)'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; todos `No Risk` | No se detectaron scripts riesgosos |
| **ADVERTENCIA** &mdash; `Low Risk` o `High Risk` presentes | Revise los scripts marcados en [TEL-5](#tel-5-análisis-detallado-de-script) |

### TEL-8: Frescura de la telemetría

**Qué demuestra:** Los datos de telemetría de CSD son recientes, confirmando monitoreo activo.

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/detected_domains" \
  | jq '{
    total_domains: .domain_summary.totalDomains.count,
    latest_update: (.domain_summary.totalDomains.lastUpdated // "unknown"),
    most_recent_domain: (.domains_list | sort_by(.latestSeenDate) | last | {
      domain: .domain,
      latest_seen: (.latestSeenDate | tonumber | todate)
    })
  }'
```

| Resultado | Significado |
| --- | --- |
| **APROBADO** &mdash; `latest_seen` está dentro de las últimas 24 horas | La telemetría está recopilando datos activamente |
| **ADVERTENCIA** &mdash; `latest_seen` es anterior a 24 horas | El tráfico puede haberse detenido o el procesamiento de CSD está retrasado |

---

## Panel de control del stack completo

Ejecute un solo comando que verifica todos los indicadores de salud críticos en cada capa y produce una tabla resumen:

```bash
echo "=== CSD Verification Dashboard ==="
echo ""

# Layer 1: DNS
DNS_A=$(dig +short xF5XC_DOMAINNAMEx A | head -1)
DNS_ACME=$(dig +short _acme-challenge.xF5XC_DOMAINNAMEx CNAME | head -1)

# Layers 2-3: LB + TLS
LB=$(curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http")
LB_HTTPS=$(curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-https")

LB_STATE=$(echo "$LB" | jq -r '.spec.state // "UNKNOWN"')
CERT_STATE=$(echo "$LB_HTTPS" | jq -r '.spec.cert_state // "UNKNOWN"')
CSD_ON_LB=$(echo "$LB" | jq -r 'if .spec.client_side_defense then "ENABLED" else "DISABLED" end')
DOMAINS=$(echo "$LB" | jq -r '.spec.domains | join(", ")')
ROUTE_POOL=$(echo "$LB" | jq -r '[.spec.default_route_pools[] | .pool.name] | join(", ")')

# Layer 6: CSD status
CSD=$(curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/status")

CSD_CONFIGURED=$(echo "$CSD" | jq -r '.isConfigured // false')
CSD_ENABLED=$(echo "$CSD" | jq -r '.isEnabled // false')

# Layer 6: JS config
JS_TAG=$(curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/js_configuration" \
  | jq -r 'if (.scriptTag | length) > 0 then "PRESENT" else "MISSING" end')

# Layer 7: Traffic (last 24h)
TRAFFIC=$(curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d "{
    \"start_time\": \"$(date -u -d '24 hours ago' +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date -u -v-24H +%Y-%m-%dT%H:%M:%SZ)\",
    \"end_time\": \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"
  }" \
  "xF5XC_API_URLx/api/data/namespaces/xF5XC_NAMESPACEx/access_logs/aggregation" \
  | jq -r '.total_hits // "0"')

printf "%-28s %s\n" "CHECK" "STATUS"
printf "%-28s %s\n" "----------------------------" "----------------------------"
printf "%-28s %s\n" "[DNS] A Record" "${DNS_A:-NOT_FOUND}"
printf "%-28s %s\n" "[DNS] ACME CNAME" "${DNS_ACME:-NOT_FOUND}"
printf "%-28s %s\n" "[TLS] Certificate" "$CERT_STATE"
printf "%-28s %s\n" "[LB] State" "$LB_STATE"
printf "%-28s %s\n" "[LB] Domains" "$DOMAINS"
printf "%-28s %s\n" "[LB] Route Pool" "$ROUTE_POOL"
printf "%-28s %s\n" "[LB] CSD on LB" "$CSD_ON_LB"
printf "%-28s %s\n" "[CSD] Configured (tenant)" "$CSD_CONFIGURED"
printf "%-28s %s\n" "[CSD] Enabled" "$CSD_ENABLED"
printf "%-28s %s\n" "[CSD] JS Script Tag" "$JS_TAG"
printf "%-28s %s\n" "[Traffic] Requests (24h)" "$TRAFFIC"
```

---

## Referencia de campos de logs de acceso

<Aside type="caution">
Cada entrada en el array `.logs[]` es una **cadena JSON**, no un objeto procesado. Debe pasar por `fromjson` en jq antes de acceder a los campos (p. ej., `.logs[] | fromjson | .method`).
</Aside>

| Campo | Tipo | Descripción |
| --- | --- | --- |
| `@timestamp` | string | Marca de tiempo de la solicitud (ISO 8601). Note el prefijo `@` &mdash; acceda con `.["@timestamp"]` en jq |
| `method` | string | Método HTTP (GET, POST, etc.) |
| `req_path` | string | Ruta URI de la solicitud |
| `rsp_code` | string | Código de estado de respuesta HTTP como cadena (p. ej., `"200"`, `"404"`) |
| `rsp_code_class` | string | Clase de código de estado (`2xx`, `3xx`, `4xx`, `5xx`, o `downstream_remote_disconnect`) |
| `src_ip` | string | Dirección IP de origen del cliente |
| `dst_ip` | string | Dirección IP de destino (VIP) |
| `domain` | string | Valor del encabezado Host de la solicitud |
| `user_agent` | string | Cadena User-Agent del cliente |
| `rsp_size` | string | Tamaño del cuerpo de la respuesta en bytes (devuelto como cadena) |
| `req_size` | string | Tamaño del cuerpo de la solicitud en bytes (devuelto como cadena) |
| `duration_with_data_tx_delay` | string | Duración total de la solicitud en segundos (devuelto como cadena, p. ej., `"0.024219"`) |
| `csd_js_injection` | string | `"true"` cuando el JavaScript de CSD fue inyectado (solo presente cuando está activo) |

## Referencia de campos de telemetría CSD

| Campo | Endpoint | Descripción |
| --- | --- | --- |
| `isConfigured` | status | CSD habilitado a nivel de tenant |
| `isEnabled` | status | CSD activo para este namespace |
| `scripts[]` | scripts | Array de objetos de scripts detectados |
| `.script_name` | scripts | URL completa del archivo JavaScript |
| `.risk_level` | scripts | Nivel de riesgo (No Risk, Low Risk, High Risk) |
| `.status` | scripts | AN (Acción necesaria) o NA (Sin acción necesaria) |
| `.form_fields_read` | scripts | Número de campos de formulario que el script lee |
| `.affected_users_count` | scripts | Número de usuarios/sesiones únicos afectados |
| `domain_summary` | detected_domains | Recuentos por estado: `.actionNeededCount.count`, `.mitigatedDomains.count`, `.allowedDomains.count`, `.totalDomains.count` (cada uno tiene `.count` y `.lastUpdated`) |
| `domains_list[]` | detected_domains | Array de objetos de dominios detectados con `.domain`, `.status`, `.category`, `.firstSeenDate`, `.latestSeenDate` (segundos epoch como cadenas) |
| `form_fields[]` | formFields | Array de objetos de campos de formulario detectados |
| `.analysis` | formFields | Clasificación de sensibilidad (Sensitive, Not Sensitive) |

## Solución de problemas

### Cero logs de acceso

Si [TV-1](#tv-1-recuento-de-solicitudes-últimas-24-horas) devuelve `0`:

<Steps>
1. **Verifique la resolución DNS** &mdash; confirme que el dominio resuelve a la VIP del LB:
   ```bash
   dig +short xF5XC_DOMAINNAMEx A
   ```
   Si está vacío, el DNS no está configurado. Consulte [Automatización API &mdash; Paso 4](/csd/demo/phase-1-build/#step-4-configure-dns).

2. **Verifique el estado del LB** &mdash; el balanceador de carga debe estar en `VIRTUAL_HOST_READY`:
   ```bash
   curl -s \
     -H "Authorization: APIToken xF5XC_API_TOKENx" \
     "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
     | jq '.spec.state'
   ```

3. **Envíe una solicitud de prueba** &mdash; genere tráfico para confirmar la conectividad de extremo a extremo:
   ```bash
   curl -sv "http://xF5XC_DOMAINNAMEx/" 2>&1 | head -20
   ```
   Busque una respuesta HTTP exitosa. Use `https://` solo si el certificado del LB HTTPS es válido.
</Steps>

### Registros gestionados deshabilitados en la zona DNS

Si [DNS-4](#dns-4-registros-gestionados-de-f5-xc-habilitados) devuelve `false`, la creación automática de registros está deshabilitada aunque F5 XC sea el proveedor DNS autoritativo. Esta es una configuración errónea común que causa que tanto el registro A como el CNAME ACME estén ausentes, lo que bloquea al balanceador de carga para alcanzar `VIRTUAL_HOST_READY` y la emisión del certificado.

Para habilitar los registros gestionados, actualice la configuración de la zona DNS para establecer `allow_http_lb_managed_records: true`. Consulte [Automatización API &mdash; Opción A: DNS gestionado por F5 XC](/csd/demo/phase-1-build/#option-a-f5-xc-managed-dns) para la llamada API.

### LB atascado en VIRTUAL_HOST_PENDING_A_RECORD

El balanceador de carga está esperando un registro A de DNS que apunte a su VIP. Consulte [LB atascado en VIRTUAL_HOST_PENDING_A_RECORD](/csd/demo/#lb-stuck-in-virtual_host_pending_a_record) para los pasos detallados de resolución.

### Certificado atascado en PreDomainChallengePending

El certificado TLS automático requiere un registro de desafío ACME. El método depende de su proveedor DNS:

**DNS gestionado por F5 XC:** Habilite `allow_http_lb_managed_records` en la zona DNS ([DNS-4](#dns-4-registros-gestionados-de-f5-xc-habilitados)). La plataforma crea automáticamente un registro de desafío ACME basado en TXT en el grupo de conjuntos de registros RR `x-ves-io-managed`. Si el certificado permanece atascado después de habilitar los registros gestionados, elimine y vuelva a crear el balanceador de carga para activar una nueva solicitud de certificado.

**DNS externo:** Cree un registro CNAME en su proveedor DNS:

```
_acme-challenge.xF5XC_DOMAINNAMEx  CNAME  *.autocerts.ves.volterra.io
```

Verifique que el registro exista:

```bash
dig +short _acme-challenge.xF5XC_DOMAINNAMEx CNAME
dig +short _acme-challenge.xF5XC_DOMAINNAMEx TXT
```

Si ambos están vacíos, el registro ACME no está configurado. El aprovisionamiento del certificado toma 5&ndash;10 minutos después de que el registro esté en su lugar. Verifique el estado de error del LB para errores específicos de validación ACME:

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx-http" \
  | jq '[.status[]? | select(.virtual_host_status != null) | .virtual_host_status.error_description]'
```

### CSD isConfigured es false

CSD debe ser habilitado a nivel de tenant por un administrador de F5 XC. Esta es una configuración a nivel de tenant que no puede configurarse a través de la API del namespace. Contacte a su administrador para habilitar Client-Side Defense.

### Array de scripts vacío

Si [TEL-1](#tel-1-inventario-de-scripts) devuelve un array vacío:

<Steps>
1. **Verifique el dominio protegido** &mdash; CSD monitorea scripts solo en dominios registrados:
   ```bash
   curl -s \
     -H "Authorization: APIToken xF5XC_API_TOKENx" \
     "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/protected_domains" \
     | jq '.items[] | {name, namespace}'
   ```

2. **Verifique la inyección JS** &mdash; confirme que la etiqueta de script CSD se está inyectando en las páginas:
   ```bash
   curl -s "http://xF5XC_DOMAINNAMEx/" | grep -oE '(zeronaught|shape)\.com[^"]*' | head -1
   ```
   Si no hay coincidencia, el JavaScript de CSD no se está inyectando. Verifique que el LB tenga habilitado `client_side_defense`.

3. **Permita tiempo de procesamiento** &mdash; después de habilitar CSD por primera vez o registrar un nuevo dominio protegido, la detección de scripts puede tomar 5&ndash;15 minutos. Genere tráfico al sitio y espere antes de volver a verificar.
</Steps>

## Referencia rápida

### Preconfiguración de rangos temporales

| Período | Desplazamiento epoch | ISO 8601 (Linux) | ISO 8601 (macOS) |
| --- | --- | --- | --- |
| 1 hora | `$(( $(date +%s) - 3600 ))` | `date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%SZ` | `date -u -v-1H +%Y-%m-%dT%H:%M:%SZ` |
| 24 horas | `$(( $(date +%s) - 86400 ))` | `date -u -d '24 hours ago' +%Y-%m-%dT%H:%M:%SZ` | `date -u -v-24H +%Y-%m-%dT%H:%M:%SZ` |
| 7 días | `$(( $(date +%s) - 604800 ))` | `date -u -d '7 days ago' +%Y-%m-%dT%H:%M:%SZ` | `date -u -v-7d +%Y-%m-%dT%H:%M:%SZ` |
| 30 días | `$(( $(date +%s) - 2592000 ))` | `date -u -d '30 days ago' +%Y-%m-%dT%H:%M:%SZ` | `date -u -v-30d +%Y-%m-%dT%H:%M:%SZ` |

### Resumen de capas de verificación

| Capa | Pruebas | Qué cubre |
| --- | --- | --- |
| [1. Resolución DNS](#capa-1-resolución-dns) | DNS-1 a DNS-4 | Registro A, CNAME ACME, autoridad del servidor de nombres, registros gestionados |
| [2. Certificado TLS](#capa-2-certificado-tls) | TLS-1 a TLS-4 | Estado del certificado, detalles del certificado, handshake en vivo, destino ACME |
| [3. Balanceador de carga HTTP](#capa-3-balanceador-de-carga-http) | LB-1 a LB-6 | Estado, dominios, indicador CSD, pools de rutas, despliegue, resumen |
| [4. Pool de origen](#capa-4-pool-de-origen) | OP-1 a OP-4 | Configuración, modo TLS, asociación HC, conectividad del origen |
| [5. Comprobación de estado](#capa-5-comprobación-de-estado) | HC-1 a HC-2 | Configuración HC, listar todos los HCs |
| [6. Configuración CSD](#capa-6-configuración-csd) | CSD-1 a CSD-5 | Estado del tenant, etiqueta JS, dominios protegidos, inyección en vivo |
| [7. Verificación de tráfico](#capa-7-verificación-de-tráfico) | TV-1 a TV-5 | Recuento de solicitudes, códigos de estado, muestras, JS en logs, prueba E2E |
| [8. Telemetría CSD](#capa-8-telemetría-csd-y-política-de-dominios) | TEL-1 a TEL-8 | Scripts, dominios, política, campos de formulario, análisis detallado, usuarios, riesgo, frescura |

### Resumen de endpoints

| Diagnóstico | Endpoint | Método | Formato de tiempo |
| --- | --- | --- | --- |
| Recuento de solicitudes | `/api/data/namespaces/\{ns\}/access_logs/aggregation` | POST | ISO 8601 |
| Códigos de estado | `/api/data/namespaces/\{ns\}/access_logs` | POST | ISO 8601 |
| Solicitudes recientes | `/api/data/namespaces/\{ns\}/access_logs` | POST | ISO 8601 |
| Estado del LB | `/api/config/namespaces/\{ns\}/http_loadbalancers/\{name\}` | GET | Ninguno |
| Pool de origen | `/api/config/namespaces/\{ns\}/origin_pools/\{name\}` | GET | Ninguno |
| Healthcheck | `/api/config/namespaces/\{ns\}/healthchecks/\{name\}` | GET | Ninguno |
| Zona DNS | `/api/config/dns/namespaces/system/dns_zones/\{zone\}` | GET | Ninguno |
| Estado CSD | `/api/shape/csd/namespaces/\{ns\}/status` | GET | Ninguno |
| Configuración JS | `/api/shape/csd/namespaces/\{ns\}/js_configuration` | GET | Ninguno |
| Dominios protegidos | `/api/shape/csd/namespaces/\{ns\}/protected_domains` | GET | Ninguno |
| Lista de scripts | `/api/shape/csd/namespaces/\{ns\}/scripts` | POST | Segundos epoch |
| Dominios detectados | `/api/shape/csd/namespaces/\{ns\}/detected_domains` | GET | Ninguno |
| Campos de formulario | `/api/shape/csd/namespaces/\{ns\}/formFields` | GET | Segundos epoch (parámetros de consulta) |
| Detalles del script | `/api/shape/csd/namespaces/\{ns\}/scripts/\{id\}/dashboard` | GET | Ninguno |
| Comportamientos del script | `/api/shape/csd/namespaces/\{ns\}/scripts/\{id\}/behaviors` | GET | Ninguno |
| Red del script | `/api/shape/csd/namespaces/\{ns\}/scripts/\{id\}/networkInteractions` | GET | Ninguno |
| Usuarios afectados | `/api/shape/csd/namespaces/\{ns\}/scripts/\{id\}/affectedUsers` | POST | Segundos epoch |

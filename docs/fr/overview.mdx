---
title: Capacités de Client-Side Defense
description: >-
  Capacités du produit, mécanismes de détection et architecture de F5 XC
  Client-Side Defense
sidebar:
  order: 1
  label: Vue d'ensemble
i18n:
  sourceHash: 8cdb2beab9a1
  translator: machine
---

import { Aside } from "@astrojs/starlight/components";

F5 Distributed Cloud Client-Side Defense (CSD) protège les applications web contre les attaques côté client en surveillant le comportement JavaScript directement dans le navigateur. Le répartiteur de charge F5 XC peut être configuré pour injecter le script de télémétrie CSD dans les pages servies au client. Ce script observe toute l'activité JavaScript — quels scripts se chargent, quels champs de formulaire ils lisent et quelles connexions réseau ils établissent. Les données de télémétrie sont envoyées à la plateforme F5 XC où des modèles d'apprentissage automatique analysent le comportement des scripts, attribuent des scores de risque et signalent les anomalies. Les équipes de sécurité examinent les détections dans la console CSD et prennent des mesures en autorisant ou en atténuant les domaines de scripts.

```mermaid
flowchart LR
    A["Browser JS"] --> B["CSD Telemetry Script"]
    B --> C["F5 XC Platform"]
    C --> D["ML Analysis"]
    D --> E["CSD Dashboard"]
```

## Signaux de détection principaux

CSD surveille trois catégories de comportement côté navigateur :

| Signal | Ce que CSD observe | Exemple |
| --- | --- | --- |
| **Lecture de champs de formulaire** | Quels scripts accèdent à quels champs `input` présents dans le DOM de la page au moment du chargement | `main.js` lisant le champ `password` sur `/login` |
| **Inventaire des scripts** | Tous les JavaScript first-party et third-party chargés sur chaque page, suivis par domaine source | Une nouvelle balise `<script>` chargeant depuis `cdn.jsdelivr.net` apparaissant sur la page de connexion |
| **Interactions réseau** | Domaines impliqués dans l'activité réseau des scripts — inclut à la fois les domaines sources de chargement de scripts et les domaines de destination fetch/XHR | Scripts provenant de `esm.sh` et cibles d'exfiltration de données comme `www.httpbin.org` apparaissant dans les domaines détectés |

<Aside type="caution">
Le signal d'interactions réseau de CSD suit principalement les **domaines sources de chargement de scripts**. Cependant, les domaines de destination fetch/XHR apparaissent également dans l'API `/detected_domains` et le tableau de domaines du Dashboard — CSD détecte l'activité réseau au niveau du domaine, pas uniquement les chargements de scripts. Voir [Limites de détection](#limites-de-détection) pour la liste complète des limitations comportementales.
</Aside>

## Matrice des fonctionnalités

| Fonctionnalité | Description | Emplacement dans la console |
| --- | --- | --- |
| **Score de risque des scripts** | Classification automatique : Aucun risque, Risque faible, Risque élevé | Script List &rarr; colonne Risk Level |
| **Sensibilité des champs de formulaire** | Classifie automatiquement les champs comme Sensibles (par le système) en fonction du type et du nom du champ | Vue Form Fields &rarr; colonne Analysis |
| **Chronologie des comportements** | Graphiques du niveau de risque du script, du domaine source et du type au fil du temps | Détail du script &rarr; Overview &rarr; Behaviors Over Time |
| **Attribution des utilisateurs affectés** | Suit les utilisateurs impactés par IP, géolocalisation, navigateur et appareil | Détail du script &rarr; onglet Affected Users |
| **Liste d'autorisation de domaines** | Marquer les domaines de scripts de confiance comme autorisés | Dashboard &rarr; ligne du domaine &rarr; Add To Allow List |
| **Liste d'atténuation de domaines** | Bloquer les appels réseau et les lectures de champs de formulaire provenant de domaines de scripts spécifiques, empêchant l'exfiltration de données | Dashboard &rarr; ligne du domaine &rarr; Add To Mitigate List |
| **Configuration des alertes** | Notifications pour les nouveaux domaines, changements de risque, comportement suspect | Section Notifications |
| **Justification des scripts** | Ajouter des notes expliquant pourquoi un script est autorisé (conformité PCI DSS) | Détail du script &rarr; champ Justification |
| **Suivi des transactions** | Compteur mensuel d'événements de télémétrie confirmant que CSD est actif | Dashboard &rarr; carte Transactions Consumed |
| **Filtres de temps et de localisation** | Filtrer toutes les vues par période (24h, 7j, 30j) et localisation | Contrôles de filtre dans la barre supérieure |

## Limites de détection

Comprendre ce que CSD ne surveille **pas** est essentiel pour définir des attentes précises lors des démonstrations :

| Limitation | Détail | Vérifié |
| --- | --- | --- |
| **Champs créés dynamiquement** | CSD suit les champs `input` présents dans le DOM au chargement de la page. Les champs injectés par JavaScript après le chargement ne sont pas surveillés. Un `<input>` créé dynamiquement et lu par un script n'apparaît pas dans la vue Form Fields. | Oui — champ absent de `/formFields` après une attente de 10 minutes |
| **Obfuscation au niveau du code** | CSD ne signale pas les techniques d'exécution dynamique de code ou les schémas d'obfuscation comme des signaux de détection distincts. Les collecteurs obfusqués produisent le même niveau de risque que les non-obfusqués — CSD suit les métadonnées comportementales, pas les schémas de code source. | Oui — « High Risk » identique pour les deux techniques |
| **Champs de formulaire en superposition** | CSD ne suit que les champs de formulaire présents dans le DOM original au chargement de la page. Les formulaires en superposition injectés par JavaScript (une technique courante de skimming numérique) ne sont pas suivis — seules les lectures des champs originaux sont détectées. | Oui — champs en superposition absents de `/formFields` après une attente de 10 minutes |
| **Comportement des compteurs du Dashboard** | Les compteurs récapitulatifs « Found &amp; Mitigated » et « Found &amp; Allowed » ne changent qu'après qu'un administrateur ajoute explicitement un domaine à la liste d'atténuation ou d'autorisation. Les compteurs « Action Needed » et « Total Found » se mettent à jour automatiquement lorsque de nouveaux domaines sont détectés. | Oui — « Found &amp; Allowed » est passé de 0 à 1 uniquement après un POST vers `/allowed_domains` |

<Aside type="note" title="Visibilité API vs Console">
Le point de terminaison API `/detected_domains` renvoie tous les domaines détectés, y compris les domaines sources de scripts first-party et third-party. Le domaine de l'application first-party (par exemple, `csd.bankexample.com`) apparaît dans la liste des domaines détectés aux côtés des domaines CDN third-party. Les domaines first-party et third-party apparaissent tous deux dans le tableau de domaines du Dashboard.

Les domaines de destination fetch/XHR (par exemple, `www.httpbin.org` contacté via `fetch()`) apparaissent également dans la réponse `/detected_domains`. La plateforme CSD les suit au niveau du domaine même s'ils ne sont pas des domaines sources de chargement de scripts.
</Aside>

## Correspondance PCI DSS v4.0

CSD répond directement à deux exigences PCI DSS v4.0 pour la sécurité des pages de paiement :

| Exigence PCI DSS | Ce qu'elle requiert | Comment CSD y répond |
| --- | --- | --- |
| **6.4.3** — Gestion des scripts sur les pages de paiement | Maintenir un inventaire de tous les scripts, fournir une autorisation écrite et une justification pour chacun, vérifier l'intégrité des scripts | Script List fournit un inventaire complet ; le champ Justification documente l'autorisation ; la chronologie des comportements suit les changements |
| **11.6.1** — Détection de falsification sur les pages de paiement | Détecter les modifications non autorisées des en-têtes HTTP et du contenu des pages de paiement | La télémétrie CSD détecte les nouvelles injections de scripts, les lectures non autorisées de champs de formulaire et les nouveaux domaines réseau — alertant sur les changements de comportement de la page |

<Aside type="tip">
Utilisez la fonctionnalité **Justification des scripts** pour documenter pourquoi chaque script est autorisé sur les pages de paiement. Cela crée une piste d'audit qui correspond directement aux exigences d'autorisation PCI DSS 6.4.3.
</Aside>

## Matrice de couverture des menaces

Le tableau suivant associe les catégories courantes d'attaques côté client aux signaux de détection CSD qui se déclencheraient pour chaque type d'attaque. Les types d'attaque marqués d'un **\*** sont confirmés par la [documentation officielle F5](https://www.f5.com/cloud/products/client-side-defense). Les types non marqués sont déduits en fonction des catégories de signaux de détection de CSD et peuvent ne pas être explicitement revendiqués par F5.

| Catégorie d'attaque | Description | Lecture de champs | Injection de script | Réseau |
| --- | --- | --- | --- | --- |
| **Formjacking** \* | Un script malveillant lit les valeurs des champs de formulaire et les exfiltre | Oui | — | Oui |
| **Skimming numérique** \* | Injecte des formulaires en superposition ou des scripts pour capturer les données de paiement | Oui | Oui | Oui |
| **Attaque de chaîne d'approvisionnement** \* | Une bibliothèque third-party compromise charge du code malveillant | — | Oui | Oui |
| **Exfiltration de données** \* | Lit les données sensibles et les envoie vers des domaines externes | Oui | — | Oui |
| **Injection de script** \* | Insère des balises `<script>` non autorisées dans la page | — | Oui | Oui |
| **Cryptojacking** \* | Injecte des scripts de minage de cryptomonnaie | — | Oui | Oui |
| **Manipulation du DOM** | Injecte ou modifie des éléments de page pour tromper les utilisateurs | — | Oui | — |
| **Man-in-the-Browser** | Intercepte les données de formulaire au sein de la session du navigateur — voir [OWASP](https://owasp.org/www-community/attacks/Man-in-the-browser_attack) et [MITRE T1185](https://attack.mitre.org/techniques/T1185/) | Oui | — | Oui |
| **Clickjacking** | Superpose des cadres invisibles pour détourner les clics des utilisateurs — voir [OWASP](https://owasp.org/www-community/attacks/Clickjacking) | — | Oui | — |
| **Persistance de web skimmer** | Réinjecte des scripts de skimming à travers les navigations de pages — voir [Sansec Magecart Research](https://sansec.io/what-is-magecart) | — | Oui | Oui |

<Aside type="note">
La détection « Réseau » couvre à la fois les domaines sources de chargement de scripts et les domaines de destination fetch/XHR — les deux apparaissent dans l'API CSD `/detected_domains` et le tableau de domaines du Dashboard. Cependant, l'atténuation CSD cible le chargement de scripts (le vecteur de chaîne d'approvisionnement), pas les appels fetch/XHR. Atténuer un domaine bloque les chargements de balises `<script>` depuis ce domaine mais n'intercepte pas les appels `fetch()` ou `XMLHttpRequest` vers celui-ci.
</Aside>

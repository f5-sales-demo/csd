---
title: Configurazione XC
description: >-
  Configurazione della piattaforma F5 XC necessaria per abilitare e gestire
  Client-Side Defense
sidebar:
  order: 2
  label: Configurazione XC
i18n:
  sourceHash: 1ca5f1c4ac1c
  translator: machine
---

import { Aside, Steps } from "@astrojs/starlight/components";
import Screenshot from "@f5xc-salesdemos/docs-theme/components/Screenshot.astro";

CSD è già abilitato sull'applicazione demo. Questa sezione mostra le schermate di configurazione.

## Navigare verso Client-Side Defense

<Steps>
1. Accedere alla **F5 Distributed Cloud Console**
2. Selezionare il workspace **Client-Side Defense**
3. Impostare il namespace su **bot-defense**
4. Navigare verso **Manage** &rarr; **Configuration**
</Steps>

<Screenshot light="/images/csd-config.png" alt="Pagina di configurazione CSD che mostra l'elenco dei domini" />

## Configurazione del dominio

L'aggiunta di un dominio nella pagina di configurazione CSD lo registra nel motore di reportistica CSD. Questo definisce l'ambito del namespace per il quale CSD raccoglie e riporta la telemetria — i rilevamenti degli script, le notifiche di avviso e le azioni di mitigazione sono tutti raggruppati sotto il dominio registrato.

Il dominio demo (`botdemo.sales-demo.f5demos.com`) è registrato qui.

<Aside type="note">
La pagina di configurazione CSD **non** controlla l'iniezione del JavaScript. L'ambito di iniezione è configurato sull'HTTP Load Balancer (vedere di seguito). Non modificare la configurazione del dominio demo — consultare le impostazioni in modalità di sola lettura.
</Aside>

## Metodo HTTP Load Balancer

L'HTTP Load Balancer è il punto in cui viene configurata l'iniezione del JavaScript. Questo controlla se e dove gli script CSD vengono inseriti nelle pagine dell'applicazione servite attraverso il load balancer.

<Steps>
1. Navigare verso **Multi-Cloud App Connect** &rarr; **HTTP Load Balancers**
2. Fare clic sul menu **&hellip;** (azioni) sul load balancer che serve l'applicazione e selezionare **Manage Configuration**

   <Screenshot light="/images/csd-lb-config.png" alt="Elenco degli HTTP Load Balancers che mostra il load balancer demo" />
3. Scorrere fino alla sezione **Client-Side Defense** nella barra laterale sinistra

   Il campo **Client-Side Defense** è impostato su **Enable** e la **Client-Side Defense Policy** mostra **Configured**.

   <Screenshot light="/images/csd-lb-csd-settings.png" alt="Toggle CSD e impostazioni all'interno della configurazione dell'HTTP Load Balancer" />
4. Fare clic su **View Configuration** per aprire i dettagli della policy CSD

   Il campo **JavaScript Insertion Settings** mostra l'ambito di iniezione. Il load balancer demo è impostato su **Insert JavaScript in All Pages**.

   <Screenshot light="/images/csd-lb-injection-scope.png" alt="Ambito di iniezione JavaScript nella sotto-configurazione CSD" />
</Steps>

<Aside type="tip">
La pagina di configurazione CSD (sopra) registra un dominio nel motore di reportistica. L'HTTP Load Balancer controlla l'iniezione degli script. Entrambi devono essere configurati affinché CSD funzioni end-to-end.
</Aside>

## Verificare l'iniezione degli script

<Steps>
1. Aprire l'[applicazione demo](https://botdemo.sales-demo.f5demos.com) in Chrome
2. Premere **F12** per aprire DevTools
3. Andare alla scheda **Elements**
4. Cercare `__imp_apg__` o `zeronaught` nell'HTML per individuare gli script di strumentazione CSD
5. Confermare che uno o più tag CSD `<script>` sono presenti nell'`<head>` — i percorsi esatti degli script variano in base al deployment
6. Confermare che gli script sono stati iniettati dal load balancer (non fanno parte del codice sorgente dell'applicazione)
</Steps>

<Screenshot light="/images/csd-injected-scripts-light.png" dark="/images/csd-injected-scripts-dark.png" alt="Script iniettati nella scheda Elements di DevTools" />

Gli script di strumentazione CSD sono iniettati dal load balancer F5 XC — non fanno parte del codice sorgente dell'applicazione.

<Aside type="caution">
Se gli script non sono presenti, verificare che il dominio sia configurato e che si stia accedendo all'applicazione attraverso il load balancer F5 XC (non direttamente all'origine).
</Aside>

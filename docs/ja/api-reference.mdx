---
title: APIリファレンス
description: CSDドメイン管理、攻撃スクリプト、緩和ルールのREST APIエンドポイント
sidebar:
  order: 10
  label: APIリファレンス
i18n:
  sourceHash: 6564ced2653f
  translator: machine
---

import { Aside } from "@astrojs/starlight/components";

CSDには、ドメイン、スクリプト、緩和、監視を管理するための専用API `/api/shape/csd/` があります。CSDは、ロードバランサー設定APIを介してHTTPロードバランサー上でも有効化できます。

## 認証

すべてのAPI呼び出しにはAPIトークンが必要です。XCコンソールの**管理** &rarr; **認証情報** &rarr; **API認証情報**でトークンを生成してください。

[APIオートメーション — 環境セットアップ](/csd/demo/#environment-setup)の説明に従って環境変数を設定してください：

```bash
set -a && source .env && set +a
```

すべてのcurlの例では`xTOKENx`プレースホルダー形式を使用しています。環境変数に置き換えるか、ページ上部のインタラクティブフォームを使用してください。例えば、`xF5XC_API_TOKENx`は`$F5XC_API_TOKEN`に対応します。

```bash
-H "Authorization: APIToken xF5XC_API_TOKENx"
```

## API規約

<Aside type="note">
これらの規約は、すべてのF5 Distributed Cloud（F5 XC）設定およびCSD APIエンドポイントに適用されます。これらを理解することで、一般的なオートメーションのミスを防ぐことができます。
</Aside>

### レスポンスボディ

- **POST**は作成されたオブジェクトをJSONとして返します。
- **PUT**および**DELETE**はHTTP 200で空の`\{\}`を返します — これは正常であり、エラーではありません。空のレスポンスボディを失敗として扱わないでください。

### リストエンドポイントとGETエンドポイント

リストエンドポイント（例：`/healthchecks`、`/origin_pools`、`/protected_domains`）と個別GETエンドポイント（例：`/healthchecks/\{name\}`）は**異なるレスポンス構造**を返します：

| エンドポイントタイプ | パスの例 | レスポンス構造 |
| --- | --- | --- |
| **リスト** | `/origin_pools` | アイテムはトップレベルの`.name`、`.namespace`、`.tenant`を持ち、`metadata: null`および`get_spec: null` |
| **個別GET** | `/origin_pools/\{name\}` | オブジェクトは`.metadata.name`、`.spec.*`を持ち、完全な設定を含む |

それぞれに正しい`jq`パスを使用してください。例えば、リストエンドポイントから名前を抽出するには`.items[].metadata.name`ではなく`.items[].name`を使用してください。

### 保護対象ドメインの識別子

保護対象ドメインのGETおよびDELETE操作では、`\{name\}`パスパラメータは任意のオブジェクト名ではなく、**ドメイン値そのもの**（例：`bankexample.com`）です。これは、名前がユーザーが選択した識別子である他のF5 XCオブジェクトとは異なります。

## CSD APIエンドポイント

ベースパス：`/api/shape/csd/namespaces/\{namespace\}/`

### ステータスと初期化

| 操作 | メソッド | パス |
| --- | --- | --- |
| CSDの有効化 | `POST` | `/api/shape/csd/namespaces/system/init` |
| ステータスの取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/status` |
| JS設定の取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/js_configuration` |
| JSのテスト | `POST` | `/api/shape/csd/namespaces/\{namespace\}/testjs` |
| ドメインの更新 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/update_domains` |

### 保護対象ドメイン

| 操作 | メソッド | パス |
| --- | --- | --- |
| 一覧取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/protected_domains` |
| 個別取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/protected_domains/\{name\}` |
| 作成 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/protected_domains` |
| 削除 | `DELETE` | `/api/shape/csd/namespaces/\{namespace\}/protected_domains/\{name\}` |

### 検出されたドメイン

| 操作 | メソッド | パス |
| --- | --- | --- |
| 検出済み一覧 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/detected_domains` |
| 詳細の取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/domain_details` |

### 許可されたドメイン

| 操作 | メソッド | パス |
| --- | --- | --- |
| 一覧取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/allowed_domains` |
| 個別取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/allowed_domains/\{name\}` |
| 作成 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/allowed_domains` |
| 削除 | `DELETE` | `/api/shape/csd/namespaces/\{namespace\}/allowed_domains/\{name\}` |

### 緩和対象ドメイン

| 操作 | メソッド | パス |
| --- | --- | --- |
| 一覧取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/mitigated_domains` |
| 個別取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/mitigated_domains/\{name\}` |
| 作成 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/mitigated_domains` |
| 削除 | `DELETE` | `/api/shape/csd/namespaces/\{namespace\}/mitigated_domains/\{name\}` |

### スクリプト

| 操作 | メソッド | パス |
| --- | --- | --- |
| スクリプト一覧 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/scripts` |
| スクリプト一覧（レガシー） | `GET` | `/api/shape/csd/namespaces/\{namespace\}/scripts` |
| スクリプト概要の取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/scripts/\{id\}/dashboard` |
| 動作一覧 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/scripts/\{id\}/behaviors` |
| ネットワークインタラクション一覧 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/scripts/\{id\}/networkInteractions` |
| 正当性の理由の更新 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/scripts/\{script_id\}/justification` |
| 正当性の理由の削除 | `DELETE` | `/api/shape/csd/namespaces/\{namespace\}/script/justification/\{justification_id\}` |
| 既読ステータスの更新 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/scripts/\{id\}/readStatus` |
| 影響を受けるユーザー一覧 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/scripts/\{script_id\}/affectedUsers` |

### フォームフィールド

| 操作 | メソッド | パス |
| --- | --- | --- |
| フォームフィールド一覧 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/formFields` |
| フォームフィールド一覧（POST） | `POST` | `/api/shape/csd/namespaces/\{namespace\}/formFields` |
| フォームフィールドの取得 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/formFields/\{id\}` |
| スクリプト別一覧 | `GET` | `/api/shape/csd/namespaces/\{namespace\}/scripts/\{id\}/formFields` |
| フィールド分析の更新 | `POST` | `/api/shape/csd/namespaces/\{namespace\}/formFields/analysis` |

## CSDの有効化

テナントのCSDを初期化します：

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/system/init"
```

## CSDステータスの取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/status" \
  | jq .
```

## 保護対象ドメイン

### 保護対象ドメインの一覧取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/protected_domains" \
  | jq .
```

### 保護対象ドメインの追加

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{
    "metadata": {
      "name": "my-app",
      "namespace": "xF5XC_NAMESPACEx"
    },
    "spec": {}
  }' \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/protected_domains" \
  | jq .
```

### 保護対象ドメインの削除

```bash
curl -s -X DELETE \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/protected_domains/my-app"
```

## 検出されたドメイン

### 検出されたドメインの一覧取得

クエリパラメータ：`locations`（ロケーションでフィルタ）、`risk`（リスクレベルでフィルタ）。

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/detected_domains" \
  | jq .
```

高リスクでフィルタ：

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/detected_domains?risk=high" \
  | jq .
```

### ドメイン詳細の取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/domain_details?name=suspicious.example.com" \
  | jq .
```

## スクリプト

### すべてのスクリプトの一覧取得

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{}' \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts" \
  | jq .
```

### スクリプト概要の取得

```bash
SCRIPT_ID="your-script-id"

curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts/${SCRIPT_ID}/dashboard" \
  | jq .
```

### スクリプト動作の一覧取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts/${SCRIPT_ID}/behaviors" \
  | jq .
```

### ネットワークインタラクションの一覧取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/scripts/${SCRIPT_ID}/networkInteractions" \
  | jq .
```

## 緩和

### 緩和対象ドメインの追加

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{
    "metadata": {
      "name": "blocked-domain",
      "namespace": "xF5XC_NAMESPACEx"
    },
    "spec": {}
  }' \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/mitigated_domains" \
  | jq .
```

### 緩和対象ドメインの一覧取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/mitigated_domains" \
  | jq .
```

### 緩和の解除

```bash
curl -s -X DELETE \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/mitigated_domains/blocked-domain"
```

## 許可されたドメイン

### 許可されたドメインの追加

```bash
curl -s -X POST \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d '{
    "metadata": {
      "name": "trusted-cdn",
      "namespace": "xF5XC_NAMESPACEx"
    },
    "spec": {}
  }' \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/allowed_domains" \
  | jq .
```

### 許可されたドメインの一覧取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/allowed_domains" \
  | jq .
```

### 許可されたドメインの削除

```bash
curl -s -X DELETE \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/allowed_domains/trusted-cdn"
```

## JSインジェクション設定

### 現在の設定の取得

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/shape/csd/namespaces/xF5XC_NAMESPACEx/js_configuration" \
  | jq .
```

## HTTPロードバランサーのCSD設定

CSD JavaScriptインジェクションは、`/api/config/namespaces/\{namespace\}/http_loadbalancers/\{name\}`の設定APIを介してHTTPロードバランサー上で直接有効化することもできます。

ロードバランサーspec内の`client_side_defense`フィールドがインジェクションを制御します：

```json
{
  "client_side_defense": {
    "policy": {
      "js_insert_all_pages": {}
    }
  }
}
```

`policy`は以下のいずれかを受け付けます：

| フィールド | 説明 |
| --- | --- |
| `js_insert_all_pages` | すべてのページにインジェクト |
| `js_insert_all_pages_except` | 除外ルールに該当するページ以外のすべてのページにインジェクト |
| `js_insertion_rules` | ルールに一致するページのみにインジェクト |
| `disable_js_insert` | インジェクトしない |

ロードバランサーでCSDを無効化するには、`client_side_defense`を`disable_client_side_defense: \{\}`に置き換えてください。

### ロードバランサーからCSD設定を読み取る

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx" \
  | jq '.spec.client_side_defense'
```

### ロードバランサーでCSDを有効化する

取得、変更、適用：

```bash
curl -s \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx" \
  > lb-config.json

jq '.spec |= (
  del(.disable_client_side_defense) |
  .client_side_defense = {
    "policy": {
      "js_insert_all_pages": {}
    }
  }
)' lb-config.json > lb-config-updated.json

curl -s -X PUT \
  -H "Authorization: APIToken xF5XC_API_TOKENx" \
  -H "Content-Type: application/json" \
  -d @lb-config-updated.json \
  "xF5XC_API_URLx/api/config/namespaces/xF5XC_NAMESPACEx/http_loadbalancers/xF5XC_LB_NAMEx" \
  | jq '.spec.client_side_defense'
```

### 除外ルールのドメインとパスのマッチング

`js_insert_all_pages_except`または`js_insertion_rules`を使用する場合、各ルールはドメインとパスの組み合わせに一致します。

**ドメイン**（いずれか1つを選択）：

| フィールド | 例 |
| --- | --- |
| `any_domain: \{\}` | すべてのドメイン |
| `domain.exact_value` | `"app.example.com"` |
| `domain.suffix_value` | `".example.com"` |
| `domain.regex_value` | `".*\\.example\\.com"` |

**パス**（いずれか1つを選択）：

| フィールド | 例 |
| --- | --- |
| `path.path` | `"/login"` |
| `path.prefix` | `"/checkout"` |
| `path.regex` | `"/user/[0-9]+"` |

## APIリファレンス

- [CSD APIリファレンス](https://docs.cloud.f5.com/docs-v2/api/shape-client-side-defense) — 完全なエンドポイントドキュメント
- [CSDの設定](https://docs.cloud.f5.com/docs-v2/client-side-defense/how-tos/configure-csd) — コンソール設定ガイド
- [API認証情報](https://docs.cloud.f5.com/docs-v2/administration/how-tos/user-mgmt/credentials) — APIトークンの生成

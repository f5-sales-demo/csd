{
  "customSets": [
    {
      "label": "API Demo",
      "description": "API-owned CSD workflow — build, attack, mitigate, and teardown; mutually exclusive with Terraform ownership",
      "paths": ["demo/**", "xc-configuration*"]
    },
    {
      "label": "Terraform",
      "description": "Terraform-owned AWS reference workflow — architecture, source, operations, verification, and teardown",
      "paths": ["terraform/**"]
    },
    {
      "label": "API Reference",
      "description": "REST API endpoints for domains, scripts, and mitigation",
      "paths": ["api-reference*"]
    }
  ],
  "promote": ["index*", "overview*"],
  "demote": ["references*", "faq*"]
}

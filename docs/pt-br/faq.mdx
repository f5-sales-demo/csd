---
title: Perguntas Frequentes
description: Perguntas frequentes sobre implantação e operação do Client-Side Defense
sidebar:
  order: 12
  label: Perguntas Frequentes
i18n:
  sourceHash: af7af812001b
  translator: machine
---

import { Aside } from "@astrojs/starlight/components";

Perguntas frequentes sobre F5 Distributed Cloud Client-Side Defense, compiladas a partir de sessões de demonstração e conversas com clientes.

## O CSD gera alertas quando violações são detectadas?

Sim, mas as regras de alerta específicas do CSD devem ser configuradas explicitamente. O CSD cria automaticamente um Receptor de Alertas usando o endereço de email da conta fornecido durante a configuração. Você pode configurar acionadores de alerta para:

- Novo domínio suspeito detectado
- Limite de pontuação de risco excedido
- Novo script detectado em uma página protegida
- Mudança de comportamento em um script visto anteriormente

Os alertas podem ser delimitados por domínio ou aplicados globalmente em todos os domínios protegidos. Consulte a seção [CSD Console — Configuração de Alertas](/csd/csd-console/#alert-configuration) para detalhes de configuração.

<Aside type="note">
As regras de alerta são configuradas no painel do CSD, não pela API de automação. Os endpoints da API (`/scripts`, `/detected_domains`) fornecem dados de detecção mas não acionam alertas por conta própria.
</Aside>

## Posso visualizar logs de requisições HTTP para eventos do CSD?

O CSD opera via **telemetria JavaScript no lado do cliente** — não há logs HTTP no servidor para detecções de script ou aplicação de mitigação. O pipeline de detecção funciona da seguinte forma:

1. A tag JavaScript do CSD (injetada pelo balanceador de carga) é executada no navegador do visitante
2. Ela monitora carregamento de scripts, mutações de DOM e acesso a campos de formulário
3. Sinais de telemetria são enviados para o backend da F5 para análise
4. Os resultados de detecção aparecem na API do CSD e no painel

Os **logs de acesso do balanceador de carga** (`/api/data/namespaces/\{ns\}/access_logs`) incluem um campo `csd_js_injection` que confirma se a tag de script do CSD foi injetada em uma resposta. Para dados de detecção do CSD, use os endpoints da API: [`/scripts`](/csd/api-reference/#detected-scripts), [`/detected_domains`](/csd/api-reference/#detected-domains) e [`/formFields`](/csd/api-reference/#form-fields).

Consulte o guia [Diagnósticos](/csd/diagnostics/) para os testes completos de verificação da Camada 7 (HTTP) e Camada 8 (telemetria do CSD).

## O CSD se integra com ferramentas SIEM (Splunk, Datadog, etc.)?

Nenhuma integração nativa de webhook SIEM ou syslog está documentada especificamente para o CSD. Os dados de detecção do CSD estão disponíveis através dos endpoints da API REST:

- `/scripts` — scripts detectados com níveis de risco
- `/detected_domains` — domínios categorizados (CDN, exfiltração, etc.)
- `/formFields` — campos de formulário acessados por scripts com classificação de sensibilidade

Os clientes podem consultar esses endpoints da API em uma agenda e encaminhar dados de detecção para seu SIEM. Consulte a [Referência da API](/csd/api-reference/) para detalhes dos endpoints e autenticação.

<Aside type="tip">
A plataforma F5 Distributed Cloud suporta configurações **Global Log Receiver** para transmitir logs de acesso HTTP para sistemas externos (Splunk, Datadog, S3, etc.). Embora isso cubra logs de acesso do balanceador de carga (incluindo o campo `csd_js_injection`), não inclui eventos de detecção do CSD. Para dados de detecção, use a abordagem de polling de API descrita acima.
</Aside>

## O que a mitigação do CSD realmente bloqueia?

A mitigação do CSD bloqueia o carregamento de tags `<script>` de domínios mitigados interceptando o atributo `src` — o JavaScript do CSD limpa o `src` para uma string vazia, prevenindo completamente a requisição de rede. Isso visa o **vetor de cadeia de suprimentos**: scripts maliciosos de terceiros que são injetados na página via tags `<script>`.

A mitigação do CSD **não** intercepta:

- chamadas `fetch()`
- chamadas `XMLHttpRequest`
- carregamentos de tags `<img>` ou `<link>`
- conexões WebSocket

Esse design é intencional — o CSD se concentra no mecanismo de carregamento de script que ataques de cadeia de suprimentos (Magecart, formjacking, skimming) usam para injetar código malicioso. Chamadas de API no nível da aplicação feitas via `fetch()` ou `XMLHttpRequest` estão fora do escopo do CSD e são manipuladas por outros recursos de segurança do F5 XC (WAF, Bot Defense, API Protection).

Consulte [Fase 3 — Mitigar](/csd/demo/phase-3-mitigate/) para uma demonstração antes/depois mostrando o comportamento de bloqueio de script.

## Quanto tempo leva para as detecções aparecerem após executar um ataque?

O tempo de detecção varia por endpoint:

| Endpoint | Tempo típico | Notas |
| --- | --- | --- |
| `/detected_domains` | Minutos | Popula primeiro — este é o indicador principal de que o pipeline do CSD está processando |
| `/scripts` | 10–30 minutos | Usa uma agenda de processamento mais lenta no backend |
| `/formFields` | 20–30 minutos | A classificação de sensibilidade ML (`Sensível` vs `Não Sensível`) requer processamento adicional |

As detecções **persistem através de desmontagem/reconstrução de infraestrutura** quando o mesmo domínio protegido é registrado novamente no mesmo tenant. Após uma desmontagem e reconstrução, consulte `/detected_domains` uma vez — se a resposta contiver itens, as detecções anteriores ainda estão disponíveis e nenhuma espera é necessária.

Consulte a seção [Fase 2 — Verificação de Detecção](/csd/demo/phase-2-attack/#step-9-detection-verification-via-api) para o protocolo de polling recomendado.
